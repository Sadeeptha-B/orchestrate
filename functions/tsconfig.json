{
  "//": "Editor-only config for the Cloudflare Pages Functions. These are bundled by Cloudflare (esbuild) and are NOT part of the app's `tsc -b` build (tsconfig.app.json only includes src/). It just gives the Worker globals (PagesFunction, KVNamespace) to your editor.",
  "compilerOptions": {
    "target": "es2022",
    "lib": ["ES2022"],
    "module": "esnext",
    "moduleResolution": "bundler",
    "types": ["@cloudflare/workers-types"],
    "skipLibCheck": true,
    "noEmit": true,
    "strict": true
  },
  "include": ["**/*.ts"]
}
