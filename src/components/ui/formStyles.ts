// Shared Tailwind class strings for form inputs/labels used across modal forms
// (HabitForm, SeasonForm, …). Keeps the visual language consistent.

export const inputClass =
    'w-full px-3 py-2 text-sm rounded-lg border border-border bg-card text-text focus:outline-none focus:ring-2 focus:ring-accent/30 focus:border-accent transition-colors';

export const labelClass = 'block text-xs font-medium text-text-light mb-1';
